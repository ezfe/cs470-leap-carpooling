/*
  These types are used by TypeScript to ensure that strings are
  verified and correct
*/

export type TripDirection = 'towards_lafayette' | 'from_lafayette'
export type UserRole = 'driver' | 'rider'
